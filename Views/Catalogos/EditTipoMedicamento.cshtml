@model Vista_historial_medico_blockchain.Models.ListadoGrupoMedicamentosDTO

@{
    ViewData["Title"] = "Home Page";
}

@using (Html.BeginForm())
{
  @Html.AntiForgeryToken()
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true"><span class="glyphicon glyphicon-remove" aria-hidden="true"></span></button>
    <h4 class="modal-title custom_align" id="Heading"> Editar tipo de Medicamento</h4>
  </div>
  <div class="modal-body">
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    @Html.HiddenFor(model => ViewData["SpecialityId"])
    <div class="form-group">
        @Html.EditorFor(model => model.Type, new { htmlAttributes = new { @class = "form-control", placeholder="Nombre"} })
        @Html.ValidationMessageFor(model => model.Type, "", new { @class = "text-danger" })
    </div>
  </div>
  <div class="modal-footer ">
    <button type="submit" class="btn btn-primary btn-lg" style="width: 100%;"><span class="glyphicon glyphicon-ok-sign"></span>Actualizar</button>
  </div>
}

<center>
  <br>
  <div class="btn-group">
  <center><data-placement="top" data-toggle="tooltip" title="regresar"><a asp-controller="Catalogos" asp-action="Medicamentos"><button style='width:140px; height:50px' class="btn btn-lg btn-primary btn-block">Regresar</button></a></center>&nbsp;&nbsp;&nbsp;
  </div>
</center>